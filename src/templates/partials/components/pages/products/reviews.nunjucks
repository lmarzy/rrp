{% if reviews %}
{% for item in reviews %}
  <blockquote>
    <p>{{ item.quote }}</p>
    <footer>
      <cite>{{ item.cite }}</cite>
    </footer>
    {% if item.link %}
      <a href="{{ item.link.url }}">{{ item.link.text }}</a>
    {% endif %}
  </blockquote>
{% endfor %}
{% endif %}
